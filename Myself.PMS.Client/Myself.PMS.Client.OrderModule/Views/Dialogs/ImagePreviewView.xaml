<UserControl x:Class="Myself.PMS.Client.OrderModule.Views.Dialogs.ImagePreviewView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Myself.PMS.Client.OrderModule.Views.Dialogs"
             mc:Ignorable="d" 
             Height="450" Width="800">
    <UserControl.Resources>
        <Style TargetType="Button">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border CornerRadius="50" Background="#3000" Name="border">
                            <ContentPresenter VerticalAlignment="Center" HorizontalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#5000"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>
    <Grid Background="#AAA">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition/>
            <ColumnDefinition Width="auto"/>
        </Grid.ColumnDefinitions>
        <Border Grid.ColumnSpan="3">
            <Border.Background>
                <ImageBrush ImageSource="{Binding CurrentImage}" Stretch="UniformToFill"/>
            </Border.Background>
        </Border>
        <Button Width="60" Height="60" Margin="10" 
            Command="{Binding FlipCommand}"
            CommandParameter="L">
            <Path Data="M10 0 0 15 10 30" Stroke="White" StrokeThickness="4"/>
        </Button>
        <Button Width="60" Height="60" Margin="10" Grid.Column="2"
            Command="{Binding FlipCommand}"
            CommandParameter="R">
            <Path Data="M0 0 10 15 0 30" Stroke="White" StrokeThickness="4"/>
        </Button>
    </Grid>
</UserControl>
